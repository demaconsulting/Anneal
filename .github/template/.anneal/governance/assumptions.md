<!-- TEMPLATE-DIRECTIVE: Curated, disprovable beliefs the design rests on. See
     `architecture-documentation.md` for the distinction between an assumption and a constraint.
     Record only load-bearing ones — if a belief were false, the shape below would be wrong.
     A belief that only constrains one system belongs in that system's decomposition rationale.
     An assumption that is disproved is a re-cut trigger, not a defect to patch.
     A new repository usually has nothing to record here yet: delete the example bullet and leave
     the list empty. Remove this block. -->

# Assumptions

Curated, disprovable truths the design rests on. `helper` reads this before re-cutting system
boundaries.

- **TODO: the belief, in a bold lead-in** — TODO: what rests on it, and what would follow if it
  turned out to be false.
