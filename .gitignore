node_modules/
package-lock.json
.venv/

# .NET build outputs, adopted from the template now that Anneal has a solution
bin/
obj/
*.user
*.suo
.vs/
*.slnx.user

# Test results written by the contract-check fixture tests and read by the contract check
artifacts/

# Generated build outputs, including compiled documents (never committed)
**/generated/
mermaid-filter.err

.agent-logs/

# Toolkit evidence: invocation records and model transcripts. Both quote repository
# content - a transcript carries the files a model was shown, a record the arguments
# it was given - so neither is ever committed. The role configuration beside them is.
.anneal/records/
.anneal/transcripts/
